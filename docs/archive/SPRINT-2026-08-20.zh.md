# 冲刺记录 —— 2026-08-19/20 通宵并行构建 + 审计

[English](SPRINT-2026-08-20.md) | 中文

一个集成者会话协调了七条以上工人车道（模型指令后为 Sonnet 5）、两个对等会话，以及收尾的深度审计。基线 `f79416bc9a` → 收盘 `05a82741e1`。本文件是持久记录；配对的审计报告在文末章节。

## 已合并（按时间顺序）

| PR | 内容 | 来源 |
|---|---|---|
| #20 | 共享前台结算抽取；重复检测门禁 7→0 克隆 | 集成者 |
| #21 | Fork CI 工作流（基于 run-gates 聚合的 3 条托管通道） | 集成者 |
| #22 | 逐文件覆盖率欠账清偿：11 个文件 121 个未覆盖位置 | 集成者 |
| #25 | plugins README 表格行 | 对等会话 |
| #26 | subagent-router 与 worktree-sandbox 的无密钥快照；过时 ast-context 金样重录（审计项 E 关闭） | 集成者 |
| #27 | 按路由 `agentOptions`（模型档位路由，套件 Δ1） | 集成者 |
| #32 | `dsh-plugin-pinned-scratchpad`（套件阶段 6） | 车道 9 |
| #39 | guard 组中的 `dsh-budget-governor`（套件阶段 7；自 plugins/ 移入——工具目录完整性守卫使该组在结构上必须承载工具） | 车道 10 + 集成者 |
| #40/#41 | Subagent 生命周期：持久报告信箱、提前的 toolName 重复认领、输出折叠（+ 目录 spec 热修） | 车道 5 |
| #42 | `dsh-plugin-diagnostic-sifter`（套件阶段 5）+ 清单/目录接线 | 车道 8 |
| #45 | 发布阻塞 S 项清扫 + subagent-report 金样刷新 | 对等会话 |
| #46 | Hook 桥对齐：continue:false 停止、session-start 门控、stop 循环上限、按会话 hook 配置 | 车道 2 |
| #52 | `AgentOptions.reasoningEffort` + 子代理继承（套件 Δ2） | 车道 5 |
| #53 | Settings：5 个 TODO 含 fail-closed 线上脱敏与原型污染加固；atomic-write fsync 持久性 | 车道 6 |
| #54 | web-fetch SSRF/私网阻断（默认开启）、apiproxy 脱敏、session-query 信任契约 | 车道 6 |
| #55 | 跨存储会话留存接缝（plan/delete，按存储的封闭联合结果） | 车道 3 |
| #56 | CI 超时因子缩放 + pwsh 金样刷新——本地快照套件全绿 | 车道 1 |

已提 issue：#23/#24（审计前 CI 类别）、#33（ast-context 挂载前作用域泄漏）、#47（rescope 残留，既有）、#48（浏览器 e2e 边界）、#49（pwsh-terminal 重复 loader id）、#50（headless LLM 重试抖动）、#51（subagent-continuable CI 余量）。

## 七阶段套件处置

阶段 1–3 验证为已上线（见[符合性笔记](../../.agents/notes/implemented/process/2026-08-20-token-suite-spec-conformance.md)）；阶段 4 按规格拒绝（LSP 接缝已提供该能力；进程内 TS provider 已排队）；阶段 5–7 当晚建成。套件增量 Δ1/Δ2 已上线（#27、#52）；Δ3 即符合性笔记。

## 审计报告（2026-08-20，合并后）

**收盘 master 的门禁矩阵：** test / test:coverage / typecheck / lint / duplication / doc-sync / build / built-invariants / website:build 按退出码全绿；test:snapshot 在 #56 后 15/15。`hygiene` 仅在 `rescope-vendor:check` 红——26 个既有残留文件（#47），冲刺未触及。结论：**无未解决的冲刺回归**；夜间两次泄漏（目录 spec 计数、报告金样）当晚发现并修复，皆因简化的合并门禁——过程教训记录如下。

**CI 真相：** master 的 Fork CI 每个失败测试都与被跟踪 issue 1:1 对应（#23/#31 类超时、#33、#48–#51）；static 通道在全部冲刺合并中保持绿色。

**代码评审：** 五个车道产出的已合并区域加集成者自身改动均经高强度评审。各区域结论记录于评审发现的 issue；评审时确认的 hooks 桥发现（UserPromptSubmit 在非提示预备步骤上触发；merge 处理中的 stopReason 丢弃；timeout-0 边界）已跟踪待修。此前开放的五个车道 PR 均在合并前评审（#52–#56）；评审驱动的显著补充：留存接缝缺失的子系统文档与生成器分类在集成时补齐。

**已排队的已知缺口（非缺陷）：** 三个新插件的组装应用无密钥快照（记录于各自 Known Limitations）、预接线的 acp-subagent-replay 场景、示例缺口 G1/G2、路由器 schema 的 `reasoningEffort` 后续，以及第二班待办（client-UI 密集叶、e2b 加固、`lsp-typescript-inprocess`、web-cordis 覆盖）。

## 过程教训（已记入记忆）

- 合并门禁必须覆盖该合并的真实影响面，否则跑全量——两次泄漏都来自"范围化测试 + 静态检查"式合并，而其改动影响了范围外的生成目录计数与示例金样。
- 用退出码验证，绝不经管道 grep——`| grep` 链两次吞掉了红色套件。
- `refs/stash` 在 git worktree 间共享：并行车道并发使用 stash 曾在两个 worktree 间交换未提交状态（已逐字节恢复）。worktree 车道禁用 `git stash`。
- Fork CI 的第一波真实价值当晚兑现：接线当晚即捕获挂载前作用域泄漏回归（#33）与过时金样类别。

## 晨间队列（仅限人工）

1. 每个删除批次一次 keel `allow publish-gate --once`：已合并分支 `feat/plugin-snapshot-coverage`、`feat/router-per-route-agent-options`、`integrate/*`（pinned-scratchpad、budget-governor、subagent-lifecycle、diagnostic-sifter、s-items、hooks-parity、reasoning-effort、settings-durability、security-sweep、retention-seam、ci-stabilization）、`lane/*`（全部八条）、`fix/release-blocker-s-items`、`audit/fixes`（其合并后）。
2. 决定：是否在 fork 上停用上游 CI/Release/Sandbox/e2e 工作流（每个 PR 上恒红的噪音）。
3. 轮换/销毁已暴露的录制 key（`sk-Zivz...Druu`）。
4. 以本记录与开放 issue（#23、#33、#47–#51）作为下一会话的待办评审。
